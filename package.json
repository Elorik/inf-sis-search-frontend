{
  "name": "inf-sis-search-frontend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev:backend": "python -m uvicorn nlp_backend.main:app --reload",
    "dev:frontend": "cd search-frontend && npm run dev",
    "dev:all": "concurrently -k -n BACK,FRONT -c auto \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^7.3.0"
  }
}
